{% extends "base.html.twig" %}
{% block title %}405 {% trans "Not allowed" %}{% endblock %}
{% block container %}
    <h1>{% trans "Not allowed" %}</h1>
    <p>{{ message|raw }}</p>
    <a href="{{ config.base_path }}/">{% trans "Return to Home Page" %}</a>
{% endblock %}
